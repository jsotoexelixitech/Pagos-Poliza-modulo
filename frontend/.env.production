# Build producción — rutas relativas (compatible con Apache ProxyPass con o sin strip)
VITE_APP_BASE=./

# QA temporal: emitir RCV sin verificar pago móvil (quitar en producción real)
VITE_SKIP_PAYMENT_VERIFY=true
